<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Camera: the composer's "take a photo" button. Without this the capture
         intent silently fails in the native app.
         NOTE (Montázs lesson): deliberately NOT declaring READ_MEDIA_IMAGES —
         declaring it triggers a Play Store photo-permissions declaration and can
         block submission. The file picker returns a URI without needing it.
         POST_NOTIFICATIONS / WAKE_LOCK / RECEIVE_BOOT_COMPLETED are merged in by
         @capacitor/local-notifications, so they're not repeated here. -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />

    <!-- Voice dictation in the composer. RECORD_AUDIO was missing, which is half
         of why the mic did nothing in the installed app; the other half was that
         Android's WebView has no Web Speech API, so it now goes through the
         phone's own speech engine (@capacitor-community/speech-recognition).
         The <queries> block is required on Android 11+ (package visibility):
         without it the plugin cannot see the device's speech service and
         available() reports false. -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <queries>
        <intent>
            <action android:name="android.speech.RecognitionService" />
        </intent>
    </queries>
</manifest>
